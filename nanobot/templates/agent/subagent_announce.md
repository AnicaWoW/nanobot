[Subagent '{{ label }}' {{ status_text }}]

This is an automated completion notice, not a message from the user. Act on it now:
finish whatever part of the original request is still open (deliver a promised
artifact via the message tool, answer the pending question, recover from a
failure), then tell the user the outcome in your own words — brief and natural,
no internals (no "subagent", no task ids, no run tokens). Do not repeat your
earlier acknowledgement: the dispatch already happened; only the outcome matters
now.

The task brief was:
{{ task }}

Result:
{{ result }}
